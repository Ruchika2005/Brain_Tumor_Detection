# Brain_Tumor_Detection
A deep learning model using the VGG16 architecture is developed to classify brain tumors from MRI images. After preprocessing, the model is trained and evaluated on separate datasets, achieving high predictive accuracy.
